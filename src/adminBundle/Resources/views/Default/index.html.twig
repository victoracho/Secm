{% extends 'base.html.twig' %}

{% block body %}
   <div class="container col-lg-10 cont-ppalintranet">
    <div class="card bg-jumbtronintranet  rounded">
        <div class="card-header">
            <i class="fa fa-angle-double-right fa-1x" aria-hidden="true"></i>&nbsp;<b>Manuales</b>        </div>
        <div class="card">
            <div class="card-body">

                <table id="tblBiblioteca" class="table table-striped table-bordered dt-responsive" cellspacing="0" width="100%">
                    <thead><tr><th width="30%">Nombre</th><th width="60%">Descripción</th><th width="10%">Descargar</th></tr></thead><tbody>

                      {% for items in item %}
                      <tr>
                    <td style="text-align: justify"><b>{{items.nombre}}</b></td>
                    <td style="text-align: justify">{{items.descripcion}}</td>
                    <td style="text-align: center"><a href="{{ asset('uploads/brochures/' ~ items.brochure) }}" download="biblioteca/manuales/manual_org.dir.gral.administacion.pdf" class=" btn  btn-info"> <i class="fa fa-file-pdf-o" aria-hidden="true"></i> <i class="fa fa-download fa-1x" aria-hidden="true"></i> </a></td>
                     </tr>
                      {% endfor %}

                  </tbody>
                </table>
            </div></div>


    </div>
  </div>
{% endblock %}
