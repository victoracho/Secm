{% extends 'layout.html.twig' %}

{% block title %}
    {{ parent() }} - Tipo de Requerimiento
{% endblock %}

{% block page_content %}





    <div class="row">
        <div class="col-md-9">
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">Mostrar Tipo Requerimiento <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> </h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table class="table table-bordered">                    <tr>
                            <td><strong>Id</strong></td>
                            <td> 
                                {{ tipoCaso.id }}
                            </td>                     
                        </tr>                    <tr>
                            <td><strong>Nombre</strong></td>
                            <td> 
                                {{ tipoCaso.nombre }}
                            </td>                     
                        </tr>                    <tr>
                            <td><strong>Descripción</strong></td>
                            <td> 
                                {{ tipoCaso.descripcion }}
                            </td>                     
                        </tr>                    <tr>
                            <td><strong>Estatus</strong></td>


                            {% if tipoCaso.status==1 %}
                                <td>Activo</td>
                            {% endif%} 
                            {% if tipoCaso.status==0 %}

                                <td>Inactivo</td>
                            {% endif%} 


                        </tr>                </table>
                </div>
                <!-- /.box-body -->
            </div>
        </div>
    </div>



    <hr/>


    <div class="form-group">
        <a class="btn btn-default" href="{{ path('tipocaso') }}">
            <span class="glyphicon glyphicon-list" aria-hidden="true"></span>
            Volver al listado
        </a>
        <a class="btn btn-primary" href="{{ path('tipocaso_edit', { 'id':  tipoCaso.id }) }}">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            Editar
        </a>
        <a class="btn btn-info" href="{{ path('tipocaso_new') }}">
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
            Nuevo
        </a>
        <!--<form action="{{ path('tipocaso_delete', { 'id': tipoCaso.id }) }}" method="post" style="display: inline-block">
            <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(delete_form) }}
        <button class="btn btn-danger" type="submit" onclick="return confirm('Are you sure');">
            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
            Eliminar
        </button>
    </form>-->
    </div>


{% endblock %}

{% block javascripts_inline %}

    {{ parent() }}
    <script src="/bundles/organizaciones/js/validaciones.js"></script>

{%endblock%}

