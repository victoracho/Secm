security:

    access_control:
        # this is a catch-all for the admin area
        # additional security lives in the controllers
        - { path: '^/fgimenez/test/', roles: ROLE_ADMIN }
        #fgimenez/test/categoria/crud
